import React from 'react';
import Button from '@material-ui/core/Button';

const SecondaryNavigationButton = () => {
    return (
        <Button variant="contained" color="secondary">
            Secondary Nav Button
        </Button>
    );
};

export default SecondaryNavigationButton;